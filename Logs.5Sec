#!/bin/bash

services=("lavad" "subspaced" "nibid")

for service in "${services[@]}"; do
    echo "Логи для служби $service протягом 5 секунд:"
    sudo journalctl -u "$service" -f -o cat &
    sleep 5
    sudo pkill -f "journalctl -u $service"
done
